// RUN: not %parser %s 2>&1 | FileCheck %s --check-prefix=CHECK-PARSER

var Number = 123;

// CHECK-PARSER:        Error: Invalid superclass. at `Number`, before: `{`
class Foo < Number {} // expect runtime error: Superclass must be a class.
