// RUN: not %parser %s 2>&1 | FileCheck %s --check-prefix=CHECK-PARSER

// CHECK-PARSER:        Error: Invalid superclass. at `foo`, before: `{`

fun foo() {}

class Subclass < foo {} // expect runtime error: Superclass must be a class.
