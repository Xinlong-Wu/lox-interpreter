// RUN: not %parser %s 2>&1 | FileCheck %s --check-prefix=CHECK-PARSER

var Nil = nil;

// CHECK-PARSER:        Error: Invalid superclass. at `Nil`, before: `{`
class Foo < Nil {} // expect runtime error: Superclass must be a class.
